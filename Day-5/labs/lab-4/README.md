# Lab 4 - Harden, Break & Restore

Start here: **[STEPS.md](STEPS.md)**

Generate certs once, then:

```bash
./scripts/generate-certs.ps1   # or .sh
docker compose up -d
```

UI: https://localhost/ (accept self-signed warning)
