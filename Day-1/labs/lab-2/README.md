# Lab 2Grafana UI Walkthrough & Administration

Start here: **[STEPS.md](STEPS.md)**

Requires Lab 1 (or start this folder’s Compose for provisioned sample dashboards).

```bash
# If using Lab 2 stack with provisioning:
docker compose up -d
```
