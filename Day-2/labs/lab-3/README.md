# Lab 3 — Host Health Visualizations

Start here: **[STEPS.md](STEPS.md)**

Uses the Lab 1 stack (no separate Compose required):

```powershell
cd c:\Users\admin\Desktop\grafana\Day-2\labs\lab-1
docker compose up -d --build
```

Then build dashboard **Host Health** in Grafana following [STEPS.md](STEPS.md).
