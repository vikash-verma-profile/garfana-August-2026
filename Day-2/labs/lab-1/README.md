# Lab 1 - Observability Stack & Data Sources

Start here: **[STEPS.md](STEPS.md)**

```bash
docker compose up -d
```

| Service | URL / Port |
|---|---|
| Grafana | http://localhost:3000 (`admin` / `admin`) |
| Prometheus | http://localhost:9090 |
| PostgreSQL | `localhost:5432` / db `demo` / `grafana` / `grafana` |
